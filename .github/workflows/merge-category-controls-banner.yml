name: Merge Category Controls Banner

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply-controls-banner:
    runs-on: ubuntu-latest

    steps:
      - name: Repository auschecken
        uses: actions/checkout@v4

      - name: Node.js einrichten
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Kategorie-Bedienbanner anwenden
        run: node scripts/applyCategoryControlsBanner.mjs

      - name: JavaScript-Syntax prüfen
        run: node --check assets/js/views/categoryControlsView.js

      - name: Änderungen committen
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add             assets/js/views/categoryControlsView.js             assets/css/tracker.css

          if git diff --cached --quiet; then
            echo "Keine Änderungen vorhanden."
          else
            git commit -m "Merge category controls into one banner"
            git push
          fi
